{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msedge",
      "request": "launch",
      "name": "Launch in Edge DevTools",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}",
      "pathMapping": {
        "/": "${workspaceFolder}"
      }
    }
  ]
}
